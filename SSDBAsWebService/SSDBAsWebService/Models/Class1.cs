using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;


    public class Class1
    {
        //if (C.size[1] == 0)
        //{
        //    F = new KeyValuePair<string, byte[]>[C.size[0]];
        //    J.Size = C.size[0];
        //    C.size[1] = (C.size[2] % C.size[0]) - C.size[0];
        //    C.size[2] = C.size[2] - (C.size[2] % C.size[0]);
        //}
        //else if (C.size[0] == C.size[1])
        //{
        //    F = new KeyValuePair<string, byte[]>[C.size[0]];
        //    C.size[1] = g;
        //    J.Size = C.size[0];
        //}

        //else if (C.size[0]> C.size[1] && C.firstitem == String.Empty)
        //{
        //    F = new KeyValuePair<string, byte[]>[C.size[1]];
        //    J.Size = C.size[1];

        //}
        //else if (C.size[0] > C.size[1] && C.firstitem != String.Empty)
        //{
        //    F = new KeyValuePair<string, byte[]>[C.size[0]];
        //    J.Size = C.size[0];

        //}

        //else if (C.size[0]< C.size[1] )
        //{
        //    F = new KeyValuePair<string, byte[]>[C.size[0]];                        
        //    J.Size = C.size[0];
        //}


        //J.FieldSizeLeft = C.size[1] - J.Size; J.FieldSizePast = J.Size + C.size[2];

        //J.FieldSizeLeft = C.size[1] + J.Size; J.FieldSizePast = C.size[2] -J.Size;

    }
